<%= form_with(model: image_artefact, local: true) do |form| %>
  <% if image_artefact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_artefact.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
        <% image_artefact.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image_file, "Image to upload:" %>
    <%= form.file_field :image_file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
