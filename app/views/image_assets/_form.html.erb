<%= form_with(model: image_asset, local: true) do |form| %>
  <% if image_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_asset.errors.count, "error") %> prohibited this image from being saved:</h2>

      <ul>
        <% image_asset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image_file, "Image to upload:" %>
    <%= form.file_field :image_file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
